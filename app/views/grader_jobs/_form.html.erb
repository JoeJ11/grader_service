<%= form_for(@grader_job) do |f| %>
  <% if @grader_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grader_job.errors.count, "error") %> prohibited this grader_job from being saved:</h2>

      <ul>
      <% @grader_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :anonym_id %><br>
    <%= f.text_field :anonym_id %>
  </div>
  <div class="field">
    <%= f.label :submission_key %><br>
    <%= f.text_field :submission_key %>
  </div>
  <div class="field">
    <%= f.label :grader_payload %><br>
    <%= f.text_area :grader_payload %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
